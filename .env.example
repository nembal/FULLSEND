# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here
DISCORD_GUILD_ID=your_guild_id_here

# Channel Configuration (comma-separated channel names)
LISTENING_CHANNELS=ideas,gtm,brainstorm
STATUS_CHANNEL=fullsend-status
IDEA_REACT_EMOJI=🎯

# Redis Configuration
REDIS_URL=redis://localhost:6379

# RabbitMQ (orchestrator task queue; roundtable publishes tasks here)
# Local: RABBITMQ_URL=amqp://localhost:5672/
# CloudAMQP (free): paste your AMQP URL below
RABBITMQ_URL=amqps://gqkialhh:zkbBpW9myshVhwpGZcQrR9qMxR-dE2WW@duck.lmq.cloudamqp.com/gqkialhh
ORCHESTRATOR_QUEUE_NAME=fullsend.orchestrator.tasks

# Environment Mode: discord | web | both
ENV=both

# Web Server Configuration
WEB_PORT=8000

# Executor = launcher for Claude Code CLI (no custom orchestration; MCP configured in Claude Code)
# Set ANTHROPIC_API_KEY to skip browser / "claude setup" — Claude Code uses it automatically
# ANTHROPIC_API_KEY=sk-ant-your-key-here
# CLAUDE_CLI_PATH=claude
# EXECUTOR_ALLOWED_TOOLS=Read,Edit,Bash
# EXECUTOR_SYSTEM_PROMPT=You are running GTM tasks. Use browser/code/API tools as needed.

# MCP (Claude Code) — used by .mcp.json when running Claude Code from this repo
# Get keys from https://www.browserbase.com/overview
# BROWSERBASE_API_KEY=your_browserbase_api_key
# BROWSERBASE_PROJECT_ID=your_browserbase_project_id
# GEMINI_API_KEY=optional_for_stagehand_vision

# Weights & Biases (W&B) — cloud (default)
# WANDB_KEY is used for W&B Inference (LLM). WANDB_API_KEY or WANDB_KEY is used for run tracking and fetch_wandb_runs.py.
# WANDB_PROJECT=gtm-demo
# WANDB_ENTITY=your_username
# WANDB_API_KEY=your_wandb_api_key
# (Only for self-hosted: WANDB_BASE_URL=http://localhost:8080)
